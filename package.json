{
  "name": "fi3ldman",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "serve": "^14.0.1"
  },
  "scripts": {
    "start": "serve site",
    "test": "playwright test",
    "test:styling": "playwright test",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
